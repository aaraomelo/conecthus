# Base URL da API (relativo ao mesmo origin quando servido via nginx/Docker).
# Em desenvolvimento local o proxy do Vite envia /api para localhost:3000.
VITE_API_URL=/api

# URL do broker MQTT (WebSocket). Vazio = usa ws(s)://<host>/mqtt (proxy/nginx).
# Local: ws://localhost:8083
VITE_MQTT_URL=